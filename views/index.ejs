<h1>Welcome to the index route!</h1>

<% boards.forEach(function(board){ %>
  <a href="/boards/<%= board._id %>">
    <div style="background:<%= board.background %>;">
      <%= board.title %>
    </div>
  </a>
<% }); %>

<a href="/boards/new">Create a new board</a>
